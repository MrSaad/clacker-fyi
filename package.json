{
  "name": "keyboard-part-picker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "NODE_ENV=production npm --prefix server start",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "sync-data": "cp ingest/keyboards.json server/data/keyboards.json"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
